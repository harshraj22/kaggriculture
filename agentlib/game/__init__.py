"""Game knowledge: constants, observation parsing, action construction, market maths.

Everything here describes the *game*. Nothing here decides anything — no policy,
no strategy, no selection. If a module in here starts making choices, it belongs
in `strategies/` or `controllers/` instead.
"""
